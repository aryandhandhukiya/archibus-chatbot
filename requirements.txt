# Web framework
streamlit==1.31.1

# Database
pymongo==4.8.0

# Google Generative AI SDK
google-generativeai==0.8.3

# NLP / Embeddings
sentence-transformers==3.0.1
torch==2.4.0
faiss-cpu==1.7.4  # CPU version; switch to faiss-gpu if using GPU EC2 instance

# Vector database
chromadb==0.5.5

# Image handling
Pillow==10.4.0

# HTTP requests
requests==2.32.3

# Environment variable management
python-dotenv==1.0.1

# BSON for ObjectId (used with pymongo)
bson==0.5.10  # sometimes not required, but included for ObjectId handling if needed outside pymongo

# Logging and utility (Python stdlib, so no pip needed):
# - re
# - os
# - io
# - json
# - datetime
# - traceback
# - base64
# - numpy

# Required for numpy (used with faiss, etc.)
numpy==1.26.4

torch==2.7.0